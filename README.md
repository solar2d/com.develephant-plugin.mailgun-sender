Sources for the plugin `plugin.mailgun-sender`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.mailgun-sender" = {
            publisherId = "com.develephant",
        },
    },
}
```
